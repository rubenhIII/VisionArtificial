# Práctica Final de Sección
## Conceptos básicos de pre-procesamiento

1. Modificar la función de procesamiento de brillo, contraste y factor gamma para que utilice el canal Y del espacio de color YUV para aplicar las operaciones.
2. Utilizar las fotografías de los diferentes tipos de iluminación (frontal, trasera, lateral izquierda y lateral derecha) para realizar ajustes de brillo, contraste y factor gamma. Elegir los valores de cada parámetro de acuerdo con su propio criterio. Guardar los valores de cada parámetro empleado.
3. Ecualizar las mismas fotografías empleadas en el punto 2 usando el método de OpenCV de ecualización de histograma (La función del archivo contraste_brillo   @staticmethod ecualizar_hist(imagen) usa este método).

### Reporte
1. Para cada ajuste realizado del ejercicio 2, por fotografía, mostrar imagen original, imagen procesada, histograma original e histograma procesado. Mostrar en el reporte los valores usados de brillo, contraste y gamma para cada imagen. Mencionar por qué se eligieron esos valores.
2. Para el ejercicio 3 mostrar imagen original, imagen procesada, histograma original e histograma de la imagen procesada.
3. Crear una carpeta en su repositorio de Github donde se incluyan los códigos, imágenes, etc. empleados para realizar la actividad. Incluir el enlace público en el reporte en una sección debidamente indicada.

El formato del reporte es IEEE. Se subirá el archivo PDF del reporte a Moodle.